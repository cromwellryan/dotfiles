#compdef '_git branch' gpp

_git_branch() {
  ref=$(git symbolic-ref HEAD 2> /dev/null)

  if [[ -n $ref ]]; then
    if (( CURRENT == 2 )); then
      compadd `git branch | sed "s/[\* ]//g"`
    fi
  fi
}

compdef _git_branch gpp

